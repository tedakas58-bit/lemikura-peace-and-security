# Install XLSX Package

To enable Excel export functionality, you need to install the xlsx package:

```bash
cd frontend
npm install xlsx @types/xlsx
```

After installation, restart your development server:

```bash
npm run dev
```

The Excel export functionality will then be available in the admin dashboard.